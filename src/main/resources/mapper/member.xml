<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.budge.hotdeal_go.model.mapper.MemberMapper">

	<!-- 원래 필요한 정보만 가져오는 것이 맞지만 디버깅용으로 다 가져와봄 -->
	<select id="findByMemberId" parameterType="MemberDto"
		resultType="MemberDto">
		select no, nickname, provider, provider_id, id, password,
		contact_email, gender, age_range, birthday, register_time,
		refresh_token
		from member
		where provider = #{provider}
		and no =
		#{no}
	</select>

	<select id="findByProviderId" parameterType="MemberDto"
		resultType="MemberDto">
		select no, nickname, provider, provider_id, id, password,
		contact_email, gender, age_range, birthday, register_time,
		refresh_token
		from member
		where provider = #{provider}
		and provider_id =
		#{provider_id}
	</select>

	<insert id="registerMember" parameterType="MemberDto">
		insert into member
		(nickname, provider, provider_id, id, password,
		contact_email,
		gender,
		age_range,
		birthday)
		values (#{nickname},
		#{provider},
		#{provider_id},
		#{id},
		#{password},
		#{contact_email},
		#{gender}, #{age_range}, #{birthday})
		<selectKey resultType="int" keyProperty="no" order="AFTER">
			select
			last_insert_id()
		</selectKey>
	</insert>

	<update id="saveRefreshToken" parameterType="map">
		update member
		set
		refresh_token = #{refresh_token}
		where no = #{no}
	</update>

	<select id="loginMember" parameterType="map"
		resultType="MemberDto">
		select no, nickname,
		provider, refresh_token
		from member
		where id =
		#{id}
		and password =
		#{password}
	</select>

	<select id="checkRefreshToken" parameterType="string"
		resultType="MemberDto">
		select no, nickname,
		provider, refresh_token
		from member
		where refresh_token = #{refreshToken}
	</select>

	<update id="logoutMember" parameterType="string">
		update member
		set
		refresh_token = NULL
		where refresh_token = #{refreshToken}
	</update>

	<delete id="withdrawMember" parameterType="string">
		delete from member
		where refresh_token = #{refreshToken}
	</delete>

</mapper>